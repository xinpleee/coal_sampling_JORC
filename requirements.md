# 煤岩地质编录自动取样规则说明

**版本：煤岩地质编录自动取样_V1.0 **  
**适用范围：煤岩地质编录自动取样程序及 tkinter 图形界面**  
**用途：程序重构、业务确认、测试验收**

---

## 1. 设计原则

1. 原始地质编录文件只读，程序不得修改或覆盖原文件。
2. 所有层级、样品和复核结果均从原始编录重新计算。
3. 自动取样结果不得存在无效区间、重复区间或样品重叠。
4. 已进入取样成果的煤层或特殊岩性，不再出现在对应复核中。
5. 无法按规则唯一处理的对象进入复核，不得静默丢弃。
6. 业务判断使用精确计算值，不使用显示时的四舍五入值。

---

## 2. 总体流程

```text
读取原始Excel
  ↓
标准化与数据校验
  ↓
识别煤层
  ↓
识别煤段
  ↓
判定煤组顶底板
  ↓
处理特殊夹层和普通夹层
  ↓
处理小于0.20米的煤层
  ↓
构建CO连续取样单元
  ↓
按1.00米上限切取煤样
  ↓
整表规范化与无重叠检查
  ↓
煤层和特殊岩性覆盖检查
  ↓
生成取样成果与分类复核
```

---

# 3. 基础数据

## 3.1 输入字段

每条原始记录至少包括：

- From
- To
- Raw Thickness
- Lithology
- Source Sheet
- Source Row

## 3.2 工作表选择

按以下顺序选择：

1. 完全匹配 `Litho【煤岩编录】`
2. 名称包含 `Litho`，不区分大小写
3. 完全匹配 `LOG`，不区分大小写
4. 未找到时停止运行并提示错误

存在多个候选工作表时，不得静默选择，必须提示用户处理。

## 3.3 字段位置

### Litho工作表

从第5行开始：

- A列：From
- B列：To
- C列：Raw Thickness
- F列：Lithology

### LOG工作表

从第2行开始：

- F列：From
- G列：To
- H列：Raw Thickness
- K列：Lithology

## 3.4 公式读取

工作簿同时以公式模式和数据模式打开：

```python
data_only=False
data_only=True
```

- 公式有缓存值时使用缓存值。
- 公式无缓存值时按字段缺失处理。
- 公式无缓存记录进入数据异常复核。

---

# 4. 数据标准化与校验

## 4.1 岩性标准化

1. 空值转为空字符串。
2. 清理首尾空格。
3. 转换为大写。
4. 各类全角或长横线统一为 `-`。
5. 删除横线两侧空格及代码内部无意义空白。

示例：

```text
 co - py  → CO-PY
```

## 4.2 深度有效性

有效记录必须满足：

```text
From和To均为有限数值
To > From
```

无效记录：

- 不参与煤层、煤段、煤组识别。
- 不参与取样。
- 写入数据异常复核。
- From或To为空时，不得参与任何数值比较。

## 4.3 厚度

```text
Calculated Thickness = To - From
```

所有规则使用计算厚度。

Raw Thickness与计算厚度差值大于0.001米时，使用计算厚度并写入复核。

## 4.4 排序

原始记录按以下顺序排序：

```text
From升序
To升序
Source Row升序
```

## 4.5 连续性

默认比较容差：

```text
EPS = 0.000001米
```

连续条件：

```text
abs(Current From - Previous To) <= EPS
```

缺口条件：

```text
Current From > Covered End + EPS
```

重叠条件：

```text
Current From < Covered End - EPS
```

深度缺口和未解决重叠会中断层级识别及自动取样。

---

# 5. 岩性分类

## 5.1 CO

岩性代码：

```text
CO
```

## 5.2 特殊岩性

默认代码：

```text
XM
ZM
CO-PY
XT
```

## 5.3 普通岩性

默认代码：

```text
CG
GV
SS
ST
MS
BMS
```

## 5.4 THIN薄夹层

非CO岩性且厚度小于或等于0.05米时，优先分类为 `THIN`。

## 5.5 普通独立夹层

普通岩性厚度满足：

```text
0.05米 < Thickness <= 0.20米
```

生成普通夹层独立样。

如果普通夹层独立样上下均与未与CO段相邻，则该普通夹层独立样不取样。

## 5.6 普通厚夹层

普通岩性厚度大于0.20米时，或上下相邻的岩性均为普通岩性，累计厚度大于0.2米时：

- 不自动取样。
- 不并入煤样。
- 中断煤段和CO连续取样单元。

---

# 6. 层级定义

## 6.1 煤层

一条或多条首尾连续的CO记录组成一个煤层。

编号：

```text
CL-001
CL-002
```

煤层中间出现任何非CO记录，即使该记录是THIN，也会结束当前煤层。

煤层厚度：

```text
Coal Layer Thickness = Coal Layer To - Coal Layer From
```

## 6.2 煤段

煤段是由以下记录首尾连续组成的最大记录块，且至少包含一条CO记录：

- CO记录
- THIN薄夹层
- 厚度大于0.05米的特殊岩性
- 上下均与CO记录相邻的厚度大于0.05米且小于或等于0.20米的普通岩性

以上记录部分先后次序。

煤段编号：

```text
CS-001
CS-002
```

以下情况结束煤段：

- 普通岩性厚度大于0.20米
- 深度缺口
- 无效记录
- 未解决重叠
- 岩性为空


## 6.3 煤组

当连续的单条或多条普通岩性记录总厚度大于2.00米，则将以该厚度区间分隔的上下煤段划分为不同煤组。

一个煤组由一个或多个煤段组成。

编号：

```text
CG-001
CG-002
```


---

# 7. 煤组顶底板规则

顶板和底板只针对煤组生成。

## 7.1 煤组顶底板取样条件

如果煤组CO累计厚度小于或等于3.00米：

```text
煤组所有记录正常取样
不取煤组顶板
不取煤组底板
```

如果煤组CO累计厚度大于3.00米：

```text
煤组所有记录正常取样
取煤组顶板
取煤组底板
```

## 7.2 顶底板样取样规则

- 目标长度为0.20米。
- 如果煤组仅有一个煤段，顶板从该煤段From向浅部延伸0.20米；如果煤组仅有多个煤段，顶板从煤组第一个煤段From向浅部延伸0.20米。
- 如果煤组仅有一个煤段，底板从该煤段To向深部延伸0.20米；如果煤组仅有多个煤段，底板从煤组最后一个煤段To向深部延伸0.20米。
- 厚度不足0.20米时，使用实际可用长度。
- 可用厚度为0时不生成样品。

### 9.4 顶底板岩性代码

顶底板样在自动取样结果的 Lithology 中使用：

```text
顶板  RF-原始岩性
底板  FL-原始岩性
```

例如：

```text
RF-ST
RF-SS

FL-ST
FL-SS
```

如果底板范围跨越多个原始岩性段：

```text
RF-ST/SS
FL-ST/SS
```


---

# 8. 特殊夹层取样

厚度大于0.05米的特殊岩性默认需要取样，并生成特殊夹层独立样。

默认边界：

```text
Sample From = 特殊岩性From
Sample To = 特殊岩性To
```

与特殊岩性直接相邻且且不与CO记录相邻的特殊岩性的THIN记录可以并入特殊夹层样。

未进入取样结果的特殊岩性记录，单独写入特殊岩性复核记录中。

特殊岩性被人工选择并入煤样后：

- 从特殊岩性复核记录中剔除。
- 作为CO连续取样单元的一部分重新分段。
- 不得切开该特殊岩性。
- 煤样总厚度仍不得超过1.00米。

---

# 9. 煤层取样规则

## 9.1 煤层厚度边界

### 小于0.20米

```text
Coal Layer Thickness < 0.20米
```

该煤层不单独生成煤样，优先自动并入相邻需要取样的特殊夹层。

### 等于或大于0.20米

```text
Coal Layer Thickness >= 0.20米
```

该煤层为需要取样的煤层，进入正常CO煤样生成流程。

## 9.2 小于0.20米煤层的自动并入THIN薄夹层

如果小于0.20米煤层与THIN薄夹层相邻，且THIN薄夹层与其他煤层紧邻，则可共同组成CO连续取样单元的一部分。

示例

```text
CO 0.3米
ST 0.02米  THIN薄夹层
CO 0.1米   小于0.20米煤层
```

则共同组成0.42米的CO连续取样单元。

如 小于0.20米煤层下方也为THIN薄夹层并入的CO段，则共同组成CO连续取样单元。

示例

```text
CO 0.3米
ST 0.02米  THIN薄夹层
CO 0.1米   小于0.20米煤层
SS 0.03米  THIN薄夹层
CO 0.19米  小于0.20米煤层
```

则共同组成0.64米的CO连续取样单元。

如共同组成CO连续取样单元小于0.2米，则该CO连续取样单元不再取样

示例

```text
CO 0.08米
ST 0.02米  THIN薄夹层
CO 0.09米   小于0.20米煤层
```

则共同组成0.19米的CO连续取样单元不再取样。

## 9.3小于0.20米煤层的自动并入特殊岩性

程序需要优先执行小于0.20米煤层的自动并入THIN薄夹层后，再执行小于0.20米煤层的自动并入特殊岩性，并入的特殊岩性为非THIN薄夹层（大于0.05米），处理顺序如下。

### 情况A：上下相邻为同种特殊岩性

```text
特殊岩性A
小于0.20米煤层
特殊岩性A
```

自动生成一个特殊夹层样：

```text
Sample From = 上层特殊岩性From
Sample To = 下层特殊岩性To
Lithology = 特殊岩性A
```

上、下特殊岩性不再分别生成两个样品。

Comments备注：

```text
10.0cm,CO
```

### 情况B：上下相邻为不同特殊岩性

```text
特殊岩性A
小于0.20米煤层
特殊岩性B
```

自动处理为：

- 上层特殊岩性A保持独立样。
- 小于0.20米煤层并入下层特殊岩性B之上。

下层样边界：

```text
Sample From = 煤层From
Sample To = 下层特殊岩性B的To
Lithology = 特殊岩性B
```

### 情况C：只有下层特殊岩性

小于0.20米煤层并入下层特殊岩性之上。

### 情况D：只有上层特殊岩性

小于0.20米煤层并入上层特殊岩性样。

### 情况E：没有直接相邻、需要取样的特殊岩性

- 煤层不自动取样。
- 写入未取样煤层复核。
- 原因代码建议使用：

```text
THIN_COAL_NO_ADJACENT_SPECIAL
```

若存在情况E，则小于0.20米煤层上下相邻的普通岩性不取样。

示例

```text
 MS  0.2米   与小于0.20米煤层上下相邻的普通岩性
 CO  0.1米   小于0.20米煤层
 MS  0.15米  与小于0.20米煤层上下相邻的普通岩性  
```
 
 则上部0.2米MS（泥岩）不取样、下部0.2米MS（泥岩）不取样。

## 9.4 自动并入成功后的状态

小于0.20米煤层成功并入THIN薄夹层或特殊夹层样后：

- 视为已取样。
- 不再出现在未取样煤层复核中。
- 对应特殊岩性视为已进入取样成果。
- 不再出现在特殊岩性未取样复核中。

---

# 10. CO煤样分段

## 10.1 CO连续取样单元

连续取样单元可以包含（无先后顺序）：

- 厚度等于或大于0.20米的煤层
- 上部和下部均与煤层紧邻的THIN薄夹层
- 符合“9.2 小于0.20米煤层的自动并入THIN薄夹层”描述的煤层
- 人工选择并入煤样的特殊岩性

CO连续取样单元的总厚度：

```text
CO连续取样单元最上部岩性记录的From-CO连续取样单元最上部岩性记录的To
```


以下对象中断CO连续取样单元：

- 独立取样的特殊夹层
- 独立取样的普通夹层
- 普通厚夹层
- 已自动并入特殊夹层样的小于0.20米煤层
- 未能并入特殊夹层的未取样煤层
- 深度缺口
- 无效记录
- 未解决重叠

**重点注意：

 只有THIN薄夹层的上部和下部均与煤层紧邻时，THIN薄夹层才能作为CO连续取样单元的一部分

例如

```text
20.0-20.6米   0.6米   CO
20.0-20.63米  0.03米  ST  THIN
20.63-21.00   0.37米  CO
21.00-21.02   0.02米  SS  THIN
```

21.00-21.02 的SS下部未与与煤层紧邻，不能作为CO连续取样单元的一部分。



## 10.2 分段硬约束

1. 若CO连续取样单元的总厚度小于等于1.5米，整段取样。
2.  若CO连续取样单元的总厚度大于1.5米，按每段煤样总厚度不得超过1.00米截取，最后剩余段的厚度不超过1.5米。
3. 若CO连续取样单元中存在大于1米且小于等于1.5米的煤层，则对该煤层完整取样。
4. 每段煤样包含的THIN累计厚度不得超过0.05米。
5. 不得切开人工选择并入煤样的特殊岩性。
6. 不得跨越CO连续取样单元边界。

## 10.3 切点

**原则1: 切点不能位于THIN内部**

算法：
初始候选切点：

```text
Candidate Cut = min(Current From + 1.00米, Unit To)
```

候选切点位于THIN内部时：

```text
Cut = THIN From
```

加入下一THIN后累计厚度将超过0.05米时：

```text
Cut = Next THIN From
```

该THIN完整进入下一煤样。

无法形成有效切点时停止当前单元分段，并写入取样规则复核。


**原则2:从CO连续取样单元的取样起始位置（第一个取样起始位置位于CO连续取样单元的最上部）按1米向下切取，切取区间内包含了n（n>=1）段煤层，若切点位于第n段煤层内部，对第n段煤层的切点按以下情况处理：**
1. 若第n段煤层厚度小于等于1米，则不产生切点；
2. 若第n段煤层厚度大于1米、小于等于1.55米，则不产生切点，且对该煤层整个区间单独取样；
3. 若下部的煤层厚度大于1.5米，则产生切点，该切点为CO连续取样单元的下一个取样起始位置。



### 10.4 每个样品中并入的其他岩性在Comments中表示

每个样品中并入的其他岩性写入 Comments，格式为：

```text
长度cm,岩性代码
```

例如：

```text
2cm,ST
```

并入多个岩性按照深度从浅到深排列，使用分号和空格分隔：

```text
2cm,ST; 4cm,SS
```


---

# 11. 人工选择与重新计算

## 11.1 特殊岩性人工并入煤样

图形界面可列出所有待复核的特殊岩性原始记录，支持按厚度筛选和批量选择。

复核字段至少包括：

- From
- To
- 厚度
- 未并入原因
- 相邻岩性

选择待复核特殊岩性原始记录并进行重新计算后：

- 并入CO连续取样单元。
- 按CO煤样分段逻辑重新分段
- 得到重新计算完整自动取样结果表后，从特殊岩性待复核样记录中删除。

## 11.2 未取样煤层复核

只有未取样的煤层进入该复核。

复核字段至少包括：

- 煤层编号
- 煤段编号
- 煤组编号
- From
- To
- 厚度
- 未并入原因
- 相邻特殊岩性

选择待复核煤样原始记录并进行重新计算后：

- 并入CO连续取样单元。
- 按CO煤样分段逻辑重新分段
- 得到重新计算完整自动取样结果表后，从煤样复核样记录中删除。
## 11.3 多次重新计算

- 第一次人工选择部分复核样后，完整重新计算。
- 后续选择在上一次已提交的选择基础上继续。
- 可以增加或取消仍在候选列表中的选择。
- 已进入取样成果的对象，不再显示在对应复核中。

---

# 13. 样品规范化

## 13.1 整表重算

任何人工选择变更后，都必须从原始编录重新生成全部样品，不得对旧成果局部追加或修改。

## 13.2 无重叠

最终样品不得满足：

```text
Current From < Previous To - EPS
```

程序在输出前必须再次检查全表。

如果自动生成结果重叠：

1. 优先回到样品归属逻辑重新计算。
2. 不能仅依靠裁剪掩盖错误归属。
3. 如仍需自动调整，必须记录调整前后边界并写入样品质量复核。
4. 调整后无有效厚度的样品删除。

## 13.3 连续同岩性样品

上下连续的非煤样同时满足以下条件时合并：

- Lithology相同
- Sample Type相同
- 前样To与后样From误差不超过EPS

煤样不得无条件合并，以免超过1.00米上限。

## 13.4 编号

程序不生成内部Sample ID。

最终样品排序后编号：

```text
S0001
S0002
S0003
```

---

# 14. 图形界面

界面使用 `tkinter`。图形界面各元素合理布局，整体采用纯白色，界面美观，增加色彩，使界面富有层次。

 图形界面上部为：原始Excel输入框和导出成果框上下并列分布，在其右侧对应布置选择文件、导出文件按钮，最右侧相对选择文件、导出文件按钮位置上级居中并排分布开始分析、按选择重新计算、参数设置按钮；
 
图形界面下部为：页面功能，面积占整个图形界面80%；在页面功能左下角用文字显示运行状态。

## 14.1 主功能

- 选择原始Excel文件
- 开始分析
- 按选择重新计算
- 参数设置
- 导出成果
- 显示运行状态
- 运行日志

## 14.2 页面

页面表格需要带有细框线，奇偶行不同颜色，奇数行为白色，偶数行为淡蓝色。
不论窗口大小变动，页面表格均铺满整个页面位置。
### 取样成果

显示：

- 样品编号
-  煤组
- 煤段
- 煤层
- From
- To
- Thickness
- Lithology
- Sample Type
- Comments

连续样人工合并功能：
1. 可对取样结果中能够对两条或多条相邻的岩性进行选择,一个选择的样品为主要对象，其他样品并入主要对象，样品的岩性为主要对象的岩性， Comments格式符合要求。
2. 右键选择合并，并整表重新计算。
3. 提供合并后向后撤销合并和向前恢复合并功能。
### 未取样特殊岩性复核

显示未取样的特殊岩性记录。

支持：

- 候选特殊岩性展示
- 厚度筛选，并批量选择
- 取消选择
- 整表重新计算

### 未取样煤层复核

显示未取样的煤层记录。


### 原始编录

显示来源行、深度、厚度、标准化岩性、分类、层级（煤层、煤段、煤组）编号、取样状态。

分类编号：

| 煤层     | CO              |
| ------ | --------------- |
| 普通岩性   | ORDINARY        |
| 特殊岩性   | SPECIAL         |
| 薄煤层夹层  | SPECIAL CO_THIN |
| 普通岩性夹层 | ORDINARY_THIN   |
| 特殊岩性夹层 | SPECIAL_THIN    |
|        |                 |

**取样状态类型与格式：**

不取样记录：不取样
取样记录：样品类型（煤样、顶板样、底板样、特殊夹层样、普通夹层样）后跟样品编号



## 14.3 参数窗口

独立参数窗口包含：

- 参数名称
- 默认值
- 当前可调值


参数名称包括：
THIN最大厚度(thin_max，米)
普通独立夹层上限(ordinary_max，米)
煤组内煤段最大间距(group_gap_max，米)
顶底板判定阈值(board_section_co max，米)
不取样煤层最大厚度(coal layer_no_sample_max，米)
顶底板目标长度(board_length，米)
单段煤样最大长度(coal_sample_max，米)
单段煤样THIN累计上限(sample thin_max，米)
厚度一致性容差(thickness tolerance，米)EPS(eps，米)
特殊岩性代码(special_codes)
普通岩性代码(ordinary_codes)



参数保存后，在下一次完整计算时生效。

---

# 15. 默认参数

| 参数                       |       默认值 | 含义           |
| ------------------------ | --------: | ------------ |
| thin_max                 |     0.05米 | THIN最大厚度     |
| ordinary_max             |     0.20米 | 普通独立夹层上限     |
| group_gap_max            |     2.00米 | 煤组内煤段最大间距    |
| board_section_co_max     |     3.00米 | 顶底板判定阈值      |
| coal_layer_no_sample_max |     0.20米 | 不取样煤层最大厚度    |
| board_length             |     0.20米 | 顶底板目标长度      |
| coal_sample_max          |     1.00米 | 单段煤样最大长度     |
| sample_thin_max          |     0.05米 | 单段煤样THIN累计上限 |
| thickness_tolerance      |    0.001米 | 厚度一致性容差      |
| EPS                      | 0.000001米 | 深度比较容差       |

---

# 16. 成果输出

程序输出新的Excel文件，不覆盖原始编录。

建议工作表：

1. 自动取样结果
2. 未取样煤层复核
3. 特殊岩性未取样复核
4. 运行记录

自动取样结果字段：

- 样品编号
- 煤组
- 煤段
- 煤层
- From
- To
- Thickness
- Lithology
- Sample Type
- Comments
- Source Record IDs

---

# 17. 关键边界

```text
煤层厚度 0.199999米：不单独取煤样，优先自动并入特殊夹层
煤层厚度 0.20米：需要取样
煤层厚度 0.200001米：需要取样
```

```text
其他岩性厚度 0.05米：THIN
其他岩性厚度 0.050001米：按特殊或普通夹层处理
```

```text
普通岩性厚度 0.20米：普通独立夹层样
普通岩性厚度 0.200001米：普通厚夹层，不自动取样
```

```text
煤样厚度 1.00米：允许
煤样厚度 > 1.00米：不允许
```

```text
煤样THIN累计 0.05米：允许
煤样THIN累计 > 0.05米：必须提前切分
```

---

# 18. 最小验收测试集

1. 0.199999米煤层上下为同种特殊岩性，自动合并为一个特殊夹层样。
2. 0.199999米煤层上下为不同特殊岩性，自动并入下层特殊岩性。
3. 0.199999米煤层只有上层特殊岩性，自动并入上层特殊岩性。
4. 0.199999米煤层只有下层特殊岩性，自动并入下层特殊岩性之上。
5. 0.199999米煤层无相邻特殊岩性，进入未取样煤层复核。
6. 0.20米煤层正常生成煤样，不进入极薄煤层复核。
7. 0.200001米煤层正常生成煤样。
8. 自动并入成功的煤层不再出现在未取样煤层复核中。
9. 自动并入成功的特殊岩性不再出现在特殊岩性未取样复核中。
10. 除特殊情况，CO样每段不超过1.00米。
11. 不切开THIN。
12. 每段THIN累计不超过0.05米。
13. 煤层被多个连续煤样联合覆盖时认定为已取样。
14. 紧邻THIN并入其他煤样时，不误报煤层未完全覆盖。
15. 最终样品无重叠。
16. 连续同岩性非煤样自动合并。
17. 连续煤样不合并为超过1.00米的样品。
18. 多次人工选择基于上一次已提交选择重新计算。
19. 更换原始文件后清空人工选择。
20. 无效深度记录不参与Decimal比较。

---

# 19. 规则优先级

1. 深度和岩性有效性
2. 原始记录重叠与缺口检查
3. 煤层、煤段、煤组识别
4. 小于0.20米煤层自动并入特殊夹层
5. 人工选择规则
6. 独立特殊夹层和普通夹层取样
7. CO连续取样单元构建
8. 不切开THIN或人工并入特殊岩性
9. 单段THIN累计不超过0.05米
10. 单段煤样不超过1.00米
11. 全表无重叠规范化
12. 煤层与特殊岩性覆盖检查
13. 成果与复核互斥
14. 排序与编号

---
